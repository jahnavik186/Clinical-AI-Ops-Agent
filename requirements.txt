# ── Python 3.14 compatible requirements ───────────────────────────────────────
# pydantic >= 2.12 is the first version with official Python 3.14 wheel support
# LangGraph/LangChain are NOT yet compatible with Python 3.14 (open issue Dec 2025)
# → replaced with custom ReAct loop in agent/orchestrator.py (zero LangChain deps)

# AWS SDK
boto3>=1.38.0
botocore>=1.38.0

# SageMaker (pure Python, no Rust extensions)
sagemaker>=2.232.0

# Data + ML — all have Python 3.14 wheels
numpy>=2.1.0
pandas>=2.2.3
scipy>=1.14.0
scikit-learn>=1.5.2
pyarrow>=18.0.0

# Pydantic — 2.12+ has official Python 3.14 support
pydantic>=2.12.0

# Amazon Bedrock direct SDK (replaces langchain-aws for Python 3.14)
anthropic>=0.40.0
httpx>=0.28.0

# Utilities
python-dotenv>=1.0.1
